<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/index.css">
    <title><%= title %></title>
</head>
<body>
    <div class="container">
        <header>
            <a href="/" class="site-logo">highland</a>
            <nav class="navigation">
                <ul>
                    <li><a href="/" class="link">Home</a></li>
                    <% if(!user) { %>
                        <li><a href="/login" class="link">Login</a></li>
                        <li><a href="/signup" class="link">Sign up</a></li>
                    <% } %>
                </ul>
                <% if(user) { %>
                    <a href="/logout" class="logout-link">Logout</a>
                    <span  class="username-span"><%= user.username %></span>
                    <img src="/images/mili-3.jpg" alt="user-profile" class="user-profile">
                <% } %>
            </nav>
        </header>

